"""UI building blocks for the DXF Tool Streamlit app.

Each module renders one region of the page (header, sidebar, tabs, ...) so
``app.py`` stays a short top-to-bottom outline of the page instead of a
single monolithic script.
"""
